// Server-only module.

import type { LiteratureProvider } from "@/lib/literature/providers/base";

/** Reserved providers — not yet wired into the search pipeline. */
export const FUTURE_LITERATURE_PROVIDERS: LiteratureProvider[] = [
];
